{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities for HR Docker Builder",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
